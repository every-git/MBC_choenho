# Database Client 비밀번호 설정 가이드

## 📍 비밀번호를 넣어야 하는 위치

Database Client 확장 프로그램의 연결 설정 파일에 비밀번호를 입력해야 합니다.

**파일 경로:**
```
~/Library/Application Support/Cursor/User/globalStorage/cweijan.vscode-database-client2/connections.json
```

---

## 🔧 방법 1: 파일 직접 편집 (가장 간단)

### 1. 파일 열기
터미널에서:
```bash
open ~/Library/Application\ Support/Cursor/User/globalStorage/cweijan.vscode-database-client2/connections.json
```

또는 Cursor에서:
- 파일 메뉴 → 파일 열기
- 위 경로 입력

### 2. 비밀번호 입력

각 연결의 `password` 필드에 비밀번호를 입력:

```json
[
  {
    "name": "Local instance 3306",
    "host": "localhost",
    "port": 3306,
    "username": "root",
    "password": "여기에_비밀번호_입력",  // ← 여기!
    "database": "dcproject",
    "driver": "MySQL",
    "connectionType": "direct",
    "ssl": false,
    "group": "Local"
  },
  {
    "name": "jdbctest",
    "host": "127.0.0.1",
    "port": 3306,
    "username": "jdbctest",
    "password": "여기에_비밀번호_입력",  // ← 여기!
    "database": "dcproject",
    ...
  },
  {
    "name": "springdbuser",
    "host": "127.0.0.1",
    "port": 3306,
    "username": "root",
    "password": "여기에_비밀번호_입력",  // ← 여기! (root 비밀번호)
    "database": "springdb",
    ...
  }
]
```

### 3. 저장 후 Cursor 재시작
- 파일 저장 (Cmd + S)
- Cursor 재시작

---

## 🔧 방법 2: Database Client GUI에서 설정

1. Cursor 왼쪽 사이드바에서 **Database Client** 아이콘 클릭
2. 연결 목록에서 연결 우클릭 (또는 설정 아이콘)
3. **Edit Connection** 선택
4. **Password** 필드에 비밀번호 입력
5. **Save** 클릭

---

## 📋 각 연결별 비밀번호

### root 계정 사용 연결들
- **Local instance 3306**: root 비밀번호
- **springdbuser**: root 비밀번호

### jdbctest 계정 사용 연결들
- **jdbctest**: jdbctest 비밀번호
- **NAS_DB**: jdbctest 비밀번호

---

## ✅ 확인 방법

비밀번호를 입력한 후:

1. Cursor 재시작
2. Database Client 아이콘 클릭
3. 연결을 클릭하여 연결 테스트
4. 연결이 성공하면 비밀번호가 올바르게 설정된 것입니다

---

## 💡 팁

- 비밀번호는 평문으로 저장됩니다 (보안상 주의)
- JSON 형식이 깨지지 않도록 따옴표 안에 넣으세요
- 연결 이름에 따라 다른 비밀번호일 수 있습니다 (root vs jdbctest)



