# Connect to Server 다이얼로그 - 화면 순서대로 입력 가이드

화면에 보이는 순서대로 정확히 설명합니다.

---

## 🔹 연결 1: Local instance 3306

### 맨 위 (연결 정보)
```
┌─────────────────────────────────────────────┐
│ Name: Local instance 3306                   │ ← 여기에 입력
│ * Group: Local                              │ ← 여기에 입력 (또는 비워두기)
│ [Advanced] [Scope: Global]                  │ ← 그대로 두기
└─────────────────────────────────────────────┘
```

### Server Type
```
MySQL (이미 선택되어 있음) ← 그대로 두기
```

### Config - Main 탭 (현재 선택된 탭)
```
┌─────────────────────────────────────────────┐
│ * Host: localhost                           │ ← "localhost" 입력
│ * Port: 3306                                │ ← 3306 (그대로 또는 + - 버튼으로 조정)
│ * Username: root                            │ ← "root" 입력
│ * Password: [비밀번호 입력]                 │ ← root 비밀번호 입력
│ Database: dcproject                         │ ← "dcproject" 입력
│ Socket Path: (비워두기)                     │ ← 비워두기
│ [Advanced] [Use]                            │ ← 필요시 사용
│                                             │
│ Features:                                   │
│ ☐ Event  ☐ Trigger                         │ ← 체크 안 해도 됨
│                                             │
│ Use Connection String: [OFF]                │ ← 끄기 (그대로)
│ SSL: [OFF]                                  │ ← 끄기 ❌ (Local instance 3306만)
└─────────────────────────────────────────────┘
```

**요약:**
- Name: `Local instance 3306`
- Group: `Local` (또는 비워두기)
- Host: `localhost`
- Port: `3306`
- Username: `root`
- Password: [실제 비밀번호]
- Database: `dcproject`
- SSL: **끄기** ❌

---

## 🔹 연결 2: jdbctest

### 맨 위
```
Name: jdbctest                                ← 여기에 입력
* Group: Local                                ← 여기에 입력 (또는 비워두기)
```

### Config - Main 탭
```
* Host: 127.0.0.1                             ← "127.0.0.1" 입력
* Port: 3306                                  ← 3306
* Username: jdbctest                          ← "jdbctest" 입력
* Password: [비밀번호 입력]                   ← jdbctest 비밀번호 입력
Database: dcproject                           ← "dcproject" 입력
Socket Path: (비워두기)                       ← 비워두기

Features:
☐ Event  ☐ Trigger                           ← 체크 안 해도 됨

Use Connection String: [OFF]                  ← 끄기
SSL: [ON]                                     ← 켜기 ✅ (토글 스위치 ON)
```

**요약:**
- Name: `jdbctest`
- Group: `Local` (또는 비워두기)
- Host: `127.0.0.1`
- Port: `3306`
- Username: `jdbctest`
- Password: [실제 비밀번호]
- Database: `dcproject`
- SSL: **켜기** ✅

---

## 🔹 연결 3: NAS_DB

### 맨 위
```
Name: NAS_DB                                  ← 여기에 입력
* Group: Remote                               ← 여기에 입력 (또는 비워두기)
```

### Config - Main 탭
```
* Host: 192.168.0.7                           ← "192.168.0.7" 입력
* Port: 3307                                  ← ⚠️ "3307" 입력 (다른 포트!)
* Username: jdbctest                          ← "jdbctest" 입력
* Password: [비밀번호 입력]                   ← jdbctest 비밀번호 입력
Database: dcproject                           ← "dcproject" 입력
Socket Path: (비워두기)                       ← 비워두기

Features:
☐ Event  ☐ Trigger                           ← 체크 안 해도 됨

Use Connection String: [OFF]                  ← 끄기
SSL: [ON]                                     ← 켜기 ✅
```

**요약:**
- Name: `NAS_DB`
- Group: `Remote` (또는 비워두기)
- Host: `192.168.0.7`
- Port: `3307` ⚠️ (다른 포트!)
- Username: `jdbctest`
- Password: [실제 비밀번호]
- Database: `dcproject`
- SSL: **켜기** ✅

---

## 🔹 연결 4: springdbuser

### 맨 위
```
Name: springdbuser                            ← 여기에 입력
* Group: Local                                ← 여기에 입력 (또는 비워두기)
```

### Config - Main 탭
```
* Host: 127.0.0.1                             ← "127.0.0.1" 입력
* Port: 3306                                  ← 3306
* Username: root                              ← "root" 입력
* Password: [비밀번호 입력]                   ← root 비밀번호 입력
Database: springdb                            ← ⚠️ "springdb" 입력 (dcproject 아님!)
Socket Path: (비워두기)                       ← 비워두기

Features:
☐ Event  ☐ Trigger                           ← 체크 안 해도 됨

Use Connection String: [OFF]                  ← 끄기
SSL: [ON]                                     ← 켜기 ✅
```

**요약:**
- Name: `springdbuser`
- Group: `Local` (또는 비워두기)
- Host: `127.0.0.1`
- Port: `3306`
- Username: `root`
- Password: [실제 비밀번호]
- Database: `springdb` ⚠️ (다른 데이터베이스!)
- SSL: **켜기** ✅

---

## 📋 한눈에 보는 비교표

| 연결 이름 | Group | Host | Port | Username | Database | SSL |
|---------|-------|------|------|----------|----------|-----|
| Local instance 3306 | Local | **localhost** | 3306 | root | dcproject | ❌ 끄기 |
| jdbctest | Local | 127.0.0.1 | 3306 | jdbctest | dcproject | ✅ 켜기 |
| NAS_DB | Remote | 192.168.0.7 | **3307** | jdbctest | dcproject | ✅ 켜기 |
| springdbuser | Local | 127.0.0.1 | 3306 | root | **springdb** | ✅ 켜기 |

---

## ⚠️ 주의할 점 3가지

1. **Local instance 3306**만 SSL 끄기 (나머지는 모두 켜기)
2. **NAS_DB**는 Port가 `3307` (나머지는 `3306`)
3. **springdbuser**는 Database가 `springdb` (나머지는 `dcproject`)

---

## 🎯 입력 순서 (화면 기준)

1. **Name** 입력 (맨 위)
2. **Group** 입력 (Name 옆, 별표 필수이지만 비워도 됨)
3. **Server Type** 확인 (MySQL 선택되어 있는지)
4. **Config - Main 탭**에서:
   - Host 입력
   - Port 입력/조정
   - Username 입력
   - Password 입력
   - Database 입력 (선택사항)
   - SSL 토글 스위치 조정
5. **+ Connect** 버튼 클릭 (또는 Save 후 연결)

---

## 💡 팁

- **Group 필드**: 별표(*)가 있어도 선택사항입니다. 비워두어도 연결 가능합니다.
- **Database 필드**: 선택사항이지만 입력하면 연결 즉시 해당 데이터베이스가 선택됩니다.
- **SSL 설정**: 로컬 개발에서는 보통 끄지만, Workbench 설정을 따르려면 위와 같이 설정하세요.




